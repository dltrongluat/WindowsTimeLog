<Page x:Class="WpfTest.ViewProject_Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfTest"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="ViewProject_Page" Loaded="Page_Loaded">
    <Canvas Margin="-25,0,-52,0" HorizontalAlignment="Right" Width="377">
        <Grid>
            <ListView Name="projectListView" Margin="-10,0,0,0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding id}"></GridViewColumn>
                        <GridViewColumn Header="Name" Width="100" DisplayMemberBinding="{Binding name}" />
                        <GridViewColumn Header="Identifier" Width="100" DisplayMemberBinding="{Binding identifier}" />
                        <GridViewColumn Header="Create Time" Width="100" DisplayMemberBinding="{Binding createdAt}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Canvas>
</Page>
